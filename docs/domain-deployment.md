# 独立域名部署

应用和静态资源部署在根路径，API 使用同域 `/api/v1/...`。配套后端配置 `PublicBasePath: ""`，否则 API 返回的音视频、歌词和海报 URL 仍会带旧前缀。

Nginx 将 `/api/` 转发至后端，保留对 `/api/v1/admin/` 的外部访问限制；其余路径交给前端。资源仍由后端通过 S3 读取，不使用宿主机数据目录直出。

根路径构建无需复制带旧前缀的 `_next` 目录。发布后验证目录 API、音频、480p/720p 视频、三种语言字幕、海报、装饰图和 Range 响应。运行凭据与实际媒体不进入 Git。
